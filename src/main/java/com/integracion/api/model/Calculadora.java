package com.integracion.api.model;

public class Calculadora {
/*
    private int resultado = 0;
    private int unusedVariable = 42;

    public void sumar(int a, int b) {
        int total = a + b;
        resultado = total;
        System.out.println("Resultado: " + total);
    }

    public void restar(int a, int b) {
        int total = a - b;
        resultado = total;
        System.out.println("Resultado: " + total);
    }

    public void multiplicar(int a, int b) {
        resultado = a * b;
        System.out.println("Resultado: " + resultado);
    }

    public void dividir(int a, int b) {
        resultado = a / b;
        System.out.println("Resultado: " + resultado);
    }

    public void operacionLarga() {
        for (int i = 0; i < 100; i++) {
            System.out.println("Procesando: " + i);
            for (int j = 0; j < 100; j++) {
                System.out.println("Subprocesando: " + j);
                for (int k = 0; k < 100; k++) {
                    System.out.println("Nivel profundo: " + k);
                }
            }
        }
    }

    public synchronized void metodoNoSeguro() {
        int x = 0;
        x++;
        x++;
        x++;
        x++;
    }

    public static void main(String[] args) {
        Calculadora c = new Calculadora();
        c.sumar(5, 3);
        c.restar(10, 2);
        c.multiplicar(3, 4);
        c.dividir(8, 0); // ⚠️ División por cero no controlada
        c.operacionLarga();
    }
    
 */
}
